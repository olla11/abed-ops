node_modules
.next
.env
.env.*
!.env.local.example
.vercel
npm-debug.log*
.DS_Store

# Gitlink auto-référencé accidentel (cassait le déploiement Cloudflare)
/abed-ops

# OpenNext (Cloudflare) — sorties de build
.open-next
.wrangler
